# State of Data Centers 2026 — Pre-Publish Claim Sheet
#
# Used by GET /api/v1/admin/qa/state-of-2026-precheck
# (routes/state_of_2026_precheck.py)
#
# Format (ONE claim per line, ALL fields required, '#' = comment):
#   claim: <verbatim narrative text> | endpoint: /api/v1/X | json_path: a.b.c | expected_min: Y | expected_max: Z
#
# Drift > 5% from claim or out-of-range = NO_GO fail.
# Lines starting with '#' or blank are ignored.

# ──────────────────────────────────────────────────────────────────────
# COVERAGE HEADLINES — what DC Hub indexes (post-2026-06-06 expansion)
# ──────────────────────────────────────────────────────────────────────

claim: 300+ markets tracked across DCPI v2.1 | endpoint: /api/v1/dcpi/scores | json_path: _total_available | expected_min: 290 | expected_max: 320
claim: 25 markets surfaced on the DCPI top-tier leaderboard | endpoint: /api/v1/dcpi/leaderboard | json_path: count | expected_min: 22 | expected_max: 28
claim: 12+ verified DCGI operators across the natural-gas index | endpoint: /api/v1/dcgi/operators | json_path: total_distinct_operators_tracked | expected_min: 10 | expected_max: 50

# ──────────────────────────────────────────────────────────────────────
# DCPI TOP-BUILD MARKET (Cheyenne, WY — composite 73.1, verdict BUILD)
# ──────────────────────────────────────────────────────────────────────

claim: Top DCPI BUILD market scores composite 73 | endpoint: /api/v1/dcpi/leaderboard | json_path: leaderboard.0.composite_score | expected_min: 65 | expected_max: 85
claim: Top BUILD market shows 70 excess power score | endpoint: /api/v1/market-brief/cheyenne | json_path: hero.excess_power | expected_min: 60 | expected_max: 80

# ──────────────────────────────────────────────────────────────────────
# AI / MCP TRACTION (your real growth story)
# ──────────────────────────────────────────────────────────────────────

claim: 50+ active dev keys across MCP integrations | endpoint: /admin/funnel-health?format=json | json_path: kpis.active_dev_keys | expected_min: 45 | expected_max: 200
claim: 35,000+ tool calls per week on the live MCP server | endpoint: /admin/funnel-health?format=json | json_path: kpis.tool_calls_7d | expected_min: 25000 | expected_max: 80000
claim: 10+ paid conversions since launch (30d window) | endpoint: /admin/funnel-health?format=json | json_path: kpis.conversions_30d | expected_min: 8 | expected_max: 100

# ──────────────────────────────────────────────────────────────────────
# ADD YOUR OWN CLAIMS BELOW
# ──────────────────────────────────────────────────────────────────────
#
# Recipe for each claim you'll cite in the LinkedIn post:
#  1. Decide your exact wording → put in `claim:`
#  2. Find the live endpoint that proves it (see cheat sheet below)
#  3. Trace json_path with `curl -H "X-Admin-Key: $KEY" <url> | python3 -m json.tool`
#  4. Set expected_min/_max with ±5–15% buffer for fresh-data drift
#
# CHEAT SHEET — endpoints proven live + their useful json_paths:
#
#   /admin/funnel-health?format=json   (admin-gated)
#       kpis.tool_calls_7d        — weekly MCP tool calls
#       kpis.conversions_30d      — paid conversions in last 30d
#       kpis.active_dev_keys      — total dev keys
#       kpis.mrr_usd              — current MRR in dollars
#       funnel.distinct_paid_users_30d  — addressable demand pool
#       signals_with_tool_tagged.pct_tagged_30d  — Fix C measurement
#       platforms[]               — array of AI platform stats
#
#   /api/v1/dcpi/scores
#       _total_available  — total DCPI market count (306)
#       count             — markets returned in page (anon-gated to 10)
#
#   /api/v1/dcpi/leaderboard
#       count             — top markets shown (25)
#       leaderboard.0.market_name  — top BUILD market name (Cheyenne)
#       leaderboard.0.composite_score  — top BUILD composite
#       leaderboard.0.verdict  — "BUILD" / "CAUTION" / "AVOID"
#
#   /api/v1/market-brief/<slug>
#       hero.excess_power     — excess power score
#       hero.constraint_score — constraint score
#       hero.iso              — ISO operator
#
#   /api/v1/dcgi/operators
#       total_distinct_operators_tracked  — 12
#
# Re-run QA after editing:
#   curl "https://dchub-backend-production.up.railway.app/api/v1/admin/qa/state-of-2026-precheck?admin_key=$KEY"
