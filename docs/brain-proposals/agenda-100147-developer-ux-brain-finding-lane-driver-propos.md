<!-- fingerprint:1d8ebe596cdd99e0dd545f2f68cf75a7 -->
**SPEC-ONLY** — this PR changes no running code and is not a fix; it captures an approved recommendation as an implementable spec.

# Brain proposal — [developer_ux] Brain finding: lane_driver_proposal @ /api/v1/admin/brain/lane-driver/state#revenue (seen

> Auto-captured from an **approved** brain agenda item (#100147). The brain's
> recommendation couldn't be expressed as a single-file edit, so it's filed here
> as a spec for a human to implement (or discard). **Draft PR — a human merges.**

_Filed 2026-07-28T18:41:16.822609Z · agenda #100147_

## The approved recommendation

Choose one: (A) approve reclassifying lane_driver_proposal out of the defect-findings pipeline into the proposal/promotion lane with (lane, metric)-keyed dedup and metric-verified closure — a one-time lifecycle change; or (B) keep it in the findings pipeline and continue per-instance fixes, accepting recurrence; and confirm whether the existing recidivism-gate work should be extended to cover this finding class rather than building a separate mechanism.

## Human checklist

- [ ] Confirm this is still worth doing
- [ ] Scope it to a concrete change (file(s) + approach)
- [ ] Implement + verify
- [ ] Or discard this PR if superseded / not worth it
