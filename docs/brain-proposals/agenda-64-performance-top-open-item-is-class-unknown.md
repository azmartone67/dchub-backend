# Brain proposal — [performance] top open item is class '(unknown)' (leverage 0.95) of 5 ranked

> Auto-captured from an **approved** brain agenda item (#64). The brain's
> recommendation couldn't be expressed as a single-file edit, so it's filed here
> as a spec for a human to implement (or close). **Draft PR — a human merges.**

_Filed 2026-07-06T09:26:57.179406Z · agenda #64_

## The approved recommendation

Choose the systemic path: (A) approve building an intake-time operator-normalization + mandatory-attribution gate and restart the stalled dedup pipeline to drain the 21,422 unverified backlog, or (B) decide the '(unknown)' class is actually a planner-taxonomy defect and commission a work-item classification schema instead. Also decide whether either fix ships via the previously recommended merge-gate or via direct operator merge — without that, the fix joins the draft graveyard.

## Human checklist

- [ ] Confirm this is still worth doing
- [ ] Scope it to a concrete change (file(s) + approach)
- [ ] Implement + verify
- [ ] Or close this PR if superseded / not worth it
