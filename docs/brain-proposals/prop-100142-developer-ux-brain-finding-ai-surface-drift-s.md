<!-- fingerprint:f7bb4dd0a3494d141cbfa79a620eb874 -->
**SPEC-ONLY** — this PR changes no running code and is not a fix; it captures an approved recommendation as an implementable spec.

# Brain proposal — [developer_ux] Brain finding: ai_surface_drift:server_card:free_tier_anon @ https://dchub.cloud/.well-kno

> Auto-captured from an **approved** brain prop item (#100142). The brain's
> recommendation couldn't be expressed as a single-file edit, so it's filed here
> as a spec for a human to implement (or discard). **Draft PR — a human merges.**

_Filed 2026-08-20T01:56:30.432054Z · prop #100142_

## The approved recommendation

Approve replacing per-field auto-fixes with a canon-driven generator + CI diff gate for all four AI surfaces (server-card.json, mcp.json, AGENTS.md, /connect), and confirm the authoritative canonical values (tier limits, version, stats) that the generator should treat as source of truth.

## Human checklist

- [ ] Confirm this is still worth doing
- [ ] Scope it to a concrete change (file(s) + approach)
- [ ] Implement + verify
- [ ] Or discard this PR if superseded / not worth it
