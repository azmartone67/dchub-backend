APScheduler==3.11.2
beautifulsoup4==4.14.3
boto3>=1.34.0
feedparser==6.0.12
Flask==3.0.0
Flask-Compress==1.23
Flask-Cors==4.0.0
google-api-core==2.29.0
google-auth>=2.35.0,<3.0
google-cloud-core==2.5.0
google-cloud-storage==2.19.0
google-crc32c==1.8.0
google-resumable-media==2.8.0
googleapis-common-protos==1.72.0
gunicorn==23.0.0
httpx==0.28.1
httpx-sse==0.4.3
lxml==6.1.0
mcp==1.26.0
oauthlib==3.3.1
PyJWT==2.12.0
python-dotenv==1.2.1
redis==7.3.0
requests==2.32.5
requests-oauthlib==2.0.0
stripe==14.1.0
psutil==7.0.0
h3
reportlab>=4.0.0
anthropic>=0.40.0
psycopg[binary]>=3.2
psycopg2-binary>=2.9
Pillow>=10.0.0
matplotlib>=3.7

# r47.3 (2026-05-25): ISO queue parsers — ERCOT PDFs (pypdf), PJM XLSX (openpyxl)
pypdf>=4.0
openpyxl>=3.1

# Market Brief PDF export (Phase 2) — HTML→PDF via weasyprint. Linux Docker
# already has pango/cairo via apt; Railway image needs libpango1.0-0 +
# libcairo2 + libpangoft2-1.0-0 + shared-mime-info installed in the
# Dockerfile (or buildpacks) for weasyprint to dlopen at runtime.
weasyprint>=60.0
