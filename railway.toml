[deploy]
startCommand = "gunicorn diag_app:app --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 60"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
