# DC Hub — Cloudflare Pages Redirects
# Updated Apr 26, 2026

# ── /ai: Pages serves ai.html automatically via pretty URLs ──
# DO NOT add "/ai /ai.html 200" — it causes an infinite redirect loop
# (Pages strips .html → 301 to /ai → rewrite to /ai.html → loop)

# ── Broken nav links → working pages ──
# Phase FF (2026-05-22): consolidate the two partner pages. /partners was a
# lighter comparison page; /partnerships is the rich "Switzerland · 6 tracks"
# resource (the keeper). 301 to one canonical so we don't dilute SEO or confuse.
/partners                      /partnerships            301
/partners/                     /partnerships            301
/markets/ashburn               /ashburn                 301
/ai.txt                        /ai.json                 301
/shell                         /powered-shell           301
/shell-rates                   /powered-shell           301
/lease-rates                   /powered-shell           301
/powered-shell-rates           /powered-shell           301
/markets/dallas-fort-worth     /markets/dallas          301
/markets/new-york-tristate     /markets/new-york        301

/intelligence                  /gdci                    301
/deals                         /transactions            301
# /transaction-comps: Pages serves automatically — no rewrite needed
/deal-flow                     /transactions            301

# ── Common variations ──
/land-power-map.html           /land-power-map          301
/login.html                    /login                   301
/pricing.html                  /pricing                 301
/upgrade                       /pricing                 302
/dashboard.html                /dashboard               301
/analytics.html                /analytics               301

# ── Landing page swap (Mar 2026) ──
/landing-page                  /                        301
/developersignup               /developers/signup       301

# ── AI agent discovery (fixed path: well-known/ not .well-known/) ──
/.well-known/ai-plugin.json    /ai-agents.json          200
# /.well-known/mcp.json  handled inline by _worker.js

# ── Common URL patterns agents try ──
/api                           /api-docs                301
/docs                          /api-docs                301
/developer                     /developers/signup       301
/developer-signup              /developers/signup       301
/dev                           /developers/signup       301
/mcp-server                    /connect                 301

# ── SEO friendly aliases ──
/facilities                    /                        200
# v7.9.10: Removed `/markets /market-intelligence 301` — it hijacked
# the /markets/ static directory (markets/index.html + 59 city pages)
# so the section appeared "disappeared" in v7.9.9.
/market-intel                  /market-intelligence     301
/pipeline                      /construction-pipeline   301
/grid                          /research/grid-intelligence/pjm/ 301
/fiber                         /land-power-map          301
/subsea                        /land-power-map          301

# ── AI page aliases (all point to /ai now) ──
/ai-hub                        /ai                      301
/for-ai                        /ai                      301
/for-agents                    /ai                      301
# /ai-integrations: Pages serves ai-integrations.html automatically
# DO NOT rewrite to .html — causes redirect loop

# ── Press release detail pages ──
# /news/<slug> → press-release (NO .html — Pages pretty-URL strips it causing 404)
/news/*                  /press-release          200
/press-release/:slug     /press-release          200

# ── Press / newsroom alias rules (added 2026-04-26) ──────────────────────
# Plural + typo variants land on the press LIST page (/press).
# Singular /press-release serves its own real content (do NOT redirect it).
# /news/* is handled above for individual articles.
/press-releases                /press                   200
/pressrelease                  /press                   301
/press-list                    /press                   301
/newsroom                      /press                   301
/media                         /press                   301
/announcements                 /press                   301
# DISABLED 2026-04-28 (was redirecting): /press-release                 /press                   301!

# (Removed /press-release -> /press 301 — press-release.html now serves its own
#  real content, no longer a dead URL. See 2026-04-20 session.)

# ── powered-shell fallback slug redirects (added 2026-04-21) ──
# /powered-shell falls back to hardcoded market slugs when its API
# (/api/v1/powered-shell/markets) 404s. Some fallback slugs don't
# match existing market pages — redirect to closest regional match
# so clicks don't 404.
# /markets/greater-philadelphia — now has its own page
# /markets/eastern-pennsylvania — now has its own page

# === phase 120: clean URLs → /api/v1/* aliases (CF worker only forwards /api/*) ===
/dcpi                       /api/v1/dcpi/page                  302
/dcpi/                      /api/v1/dcpi/page                  302
/dcpi/og/:slug.svg          /api/v1/dcpi/og/:slug.svg          302
/dcpi/og/:slug              /api/v1/dcpi/og/:slug              302
/dcpi/embed/:slug           /api/v1/dcpi/embed/:slug           302
/dcpi/press                 /api/v1/dcpi/press                 302
/dcpi/:slug                 /api/v1/dcpi/page/:slug            302
/data                       /api/v1/data                       302
/data/dcpi-current.json     /api/v1/data/dcpi-current.json     302
/data/dcpi-history.csv      /api/v1/data/dcpi-history.csv      302
/research                   /api/v1/research                   302
/research/:slug             /api/v1/research/:slug             302
/heartbeat                  /api/v1/heartbeat/page             302
/lab                        /api/v1/lab/page                   302
/markets    /market-intelligence    301
