<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Data Center Markets | 59 Global Markets · Live Intelligence | DC Hub</title>
<meta name="description" content="Live intelligence for 59 global data center markets. Real-time power pricing, capacity, pipeline, GDCI rankings, and grid status across Americas, EMEA, APAC, LATAM, and Africa.">
<meta name="keywords" content="data center markets, colocation markets, data center capacity, power costs, site selection, hyperscale markets, FLAPD, AI infrastructure, global data center index, GDCI">
<link rel="canonical" href="https://dchub.cloud/markets/">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Instrument+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<link rel="stylesheet" href="/css/market-page.css">
<meta property="og:title" content="Data Center Markets | 59 Global Markets | DC Hub">
<meta property="og:description" content="Live intelligence for 59 global data center markets: power, capacity, pipeline, GDCI rankings.">
<meta property="og:type" content="website">
<meta property="og:url" content="https://dchub.cloud/markets/">
<meta property="og:image" content="https://dchub.cloud/og-default.png">
<script type="application/ld+json">{"@context":"https://schema.org","@type":"CollectionPage","name":"Data Center Markets","description":"Live intelligence for 59 global data center markets","url":"https://dchub.cloud/markets/","publisher":{"@type":"Organization","name":"DC Hub","url":"https://dchub.cloud"},"breadcrumb":{"@type":"BreadcrumbList","itemListElement":[{"@type":"ListItem","position":1,"name":"DC Hub","item":"https://dchub.cloud"},{"@type":"ListItem","position":2,"name":"Markets","item":"https://dchub.cloud/markets/"}]}}</script>
<style>
.mx-hero{max-width:1400px;margin:0 auto;padding:2.5rem 2rem 1.5rem}
.mx-hero h1{font-size:2.4rem;font-weight:800;letter-spacing:-0.02em;margin-bottom:.5rem}
.mx-hero .sub{color:var(--ts);font-size:1.05rem;max-width:900px;margin-bottom:1.5rem}
.mx-kpis{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:.75rem;margin-bottom:2rem}
.mx-kpi{background:var(--c);border:1px solid var(--b);border-radius:10px;padding:1rem}
.mx-kpi .v{font-size:1.5rem;font-weight:800;color:var(--a);letter-spacing:-0.02em}
.mx-kpi .l{font-size:.72rem;color:var(--tm);text-transform:uppercase;letter-spacing:.05em;margin-top:.25rem}
.mx-kpi .d{font-size:.72rem;color:var(--ok);margin-top:.25rem}
.mx-kpi .d.down{color:var(--bad)}

.mx-controls{max-width:1400px;margin:0 auto;padding:0 2rem 1rem;display:flex;gap:.75rem;flex-wrap:wrap;align-items:center}
.mx-search{flex:1;min-width:240px;background:var(--c);border:1px solid var(--b);color:var(--t);padding:.7rem 1rem;border-radius:10px;font-size:.9rem;font-family:inherit}
.mx-search:focus{outline:none;border-color:var(--a)}
.mx-sort,.mx-filter{background:var(--c);border:1px solid var(--b);color:var(--t);padding:.7rem .9rem;border-radius:10px;font-size:.85rem;font-family:inherit;cursor:pointer}
.mx-chips{max-width:1400px;margin:0 auto;padding:0 2rem 1rem;display:flex;gap:.4rem;flex-wrap:wrap}
.chip{background:var(--c);border:1px solid var(--b);color:var(--ts);padding:.35rem .8rem;border-radius:20px;font-size:.75rem;font-weight:600;cursor:pointer;text-transform:uppercase;letter-spacing:.04em;transition:all .15s}
.chip:hover{color:var(--t);border-color:rgba(59,130,246,.4)}
.chip.active{background:var(--ag);color:var(--a);border-color:var(--a)}

.mx-grid{max-width:1400px;margin:0 auto;padding:0 2rem 3rem;display:grid;grid-template-columns:repeat(auto-fill,minmax(320px,1fr));gap:1rem}
.mkt-card{background:var(--c);border:1px solid var(--b);border-radius:12px;padding:1.25rem;text-decoration:none;color:var(--t);display:flex;flex-direction:column;gap:.6rem;transition:all .15s;position:relative}
.mkt-card:hover{border-color:var(--a);transform:translateY(-1px)}
.mkt-top{display:flex;align-items:center;gap:.5rem;flex-wrap:wrap}
.mkt-flag{font-size:1.4rem}
.mkt-name{font-size:1.1rem;font-weight:700;letter-spacing:-0.01em}
.mkt-tier{font-size:.65rem;font-weight:700;padding:.2rem .5rem;border-radius:4px;text-transform:uppercase;letter-spacing:.04em}
.mkt-tl{font-size:.85rem;color:var(--a);font-weight:600}
.mkt-desc{font-size:.8rem;color:var(--ts);line-height:1.45;flex-grow:1}
.mkt-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:.5rem;margin-top:.5rem}
.mkt-stat{background:var(--bg);border-radius:6px;padding:.5rem .4rem;text-align:center}
.mkt-stat .mv{font-size:.85rem;font-weight:700}
.mkt-stat .ml{font-size:.6rem;color:var(--tm);text-transform:uppercase;letter-spacing:.05em}
.mkt-badge{position:absolute;top:.75rem;right:.75rem;font-size:.65rem;padding:.15rem .45rem;border-radius:4px;font-weight:700}
.mkt-up{background:rgba(34,197,94,.12);color:var(--ok)}
.mkt-hot{background:rgba(239,68,68,.12);color:var(--bad)}
.mkt-hidden{display:none!important}
.mx-empty{grid-column:1/-1;text-align:center;padding:3rem;color:var(--tm)}
</style>
<script src="/js/dchub-nav.js" defer></script>
</head>
<body>
<nav class="dchub-nav"><div class="ni" style="max-width:1400px;margin:0 auto;display:flex;align-items:center;justify-content:space-between;height:60px;padding:0 2rem"><a href="/" style="font-size:1.25rem;font-weight:700;color:#fafafa;text-decoration:none">DC <span style="color:#3b82f6">Hub</span></a><ul style="display:flex;gap:1.5rem;list-style:none;margin:0;padding:0"><li><a href="/" style="color:#8a8a95;text-decoration:none;font-size:.9rem">Map</a></li><li><a href="/land-power" style="color:#8a8a95;text-decoration:none;font-size:.9rem">Land &amp; Power</a></li><li><a href="/markets/" style="color:#3b82f6;text-decoration:none;font-size:.9rem;font-weight:600">Markets</a></li><li><a href="/gdci" style="color:#8a8a95;text-decoration:none;font-size:.9rem">GDCI</a></li><li><a href="/research/grid-intelligence/" style="color:#8a8a95;text-decoration:none;font-size:.9rem">Grid Intel</a></li><li><a href="/pricing" style="color:#8a8a95;text-decoration:none;font-size:.9rem">Pricing</a></li></ul></div></nav>

<div class="bc" style="max-width:1400px"><a href="/">DC Hub</a> › Markets</div>

<section class="mx-hero">
  <h1>Global Data Center Markets</h1>
  <p class="sub">Live intelligence across <strong id="mx-count">59</strong> markets. Each page pulls real-time facility counts, grid status, energy pricing, pipeline MW, tax incentives, and news via the DC Hub MCP backend.</p>
  <div class="mx-kpis" id="mx-kpis">
    <div class="mx-kpi"><div class="v" id="kpi-markets">59</div><div class="l">Tracked Markets</div></div>
    <div class="mx-kpi"><div class="v" id="kpi-facilities">20,000+</div><div class="l">Facilities Indexed</div></div>
    <div class="mx-kpi"><div class="v" id="kpi-pipeline">—</div><div class="l">Global Pipeline</div></div>
    <div class="mx-kpi"><div class="v" id="kpi-movers">—</div><div class="l">Weekly Movers</div></div>
    <div class="mx-kpi"><div class="v" id="kpi-updated">Live</div><div class="l">Data Freshness</div></div>
  </div>
</section>

<div class="mx-chips" id="mx-chips">
  <span class="chip active" data-tier="all">All</span>
  <span class="chip" data-tier="us-primary">US Primary</span>
  <span class="chip" data-tier="us-secondary">US Secondary</span>
  <span class="chip" data-tier="us-emerging">US Emerging</span>
  <span class="chip" data-tier="canada">Canada</span>
  <span class="chip" data-tier="europe-flapd">Europe FLAPD</span>
  <span class="chip" data-tier="europe-emerging">Europe Emerging</span>
  <span class="chip" data-tier="nordics">Nordics</span>
  <span class="chip" data-tier="apac-primary">APAC Primary</span>
  <span class="chip" data-tier="apac-emerging">APAC Emerging</span>
  <span class="chip" data-tier="middle-east">Middle East</span>
  <span class="chip" data-tier="latam">LATAM</span>
  <span class="chip" data-tier="africa">Africa</span>
</div>

<div class="mx-controls">
  <input class="mx-search" id="mx-search" placeholder="Search markets, operators, alt names (e.g. 'slough', 'SUPERNAP', 'Johor')…" autocomplete="off">
  <select class="mx-sort" id="mx-sort">
    <option value="tier">Sort: Tier</option>
    <option value="alpha">Sort: A → Z</option>
    <option value="gdci">Sort: GDCI score</option>
    <option value="pipeline">Sort: Pipeline MW</option>
    <option value="price">Sort: Power cost</option>
  </select>
</div>

<div class="mx-grid" id="mx-grid">
  <div class="mx-empty">Loading markets…</div>
</div>

<footer>© 2026 DC Hub · Live data via DC Hub MCP · <a href="/about">About</a> · <a href="/api-docs">API</a> · <a href="/faq">FAQ</a> · <a href="/glossary">Glossary</a></footer>

<script>
(async function () {
  'use strict';
  const API = '/api/v1';
  const grid = document.getElementById('mx-grid');
  const searchEl = document.getElementById('mx-search');
  const sortEl = document.getElementById('mx-sort');
  const chipsEl = document.getElementById('mx-chips');

  let state = { tier: 'all', q: '', sort: 'tier' };

  // --- Fetch registry + live stats in parallel ---
  const [regResp, listResp, gdciResp] = await Promise.all([
    fetch('/markets/registry.json').then(r => r.json()).catch(() => null),
    fetch(API + '/markets/list').then(r => r.ok ? r.json() : null).catch(() => null),
    fetch(API + '/gdci?top=10').then(r => r.ok ? r.json() : null).catch(() => null),
  ]);

  if (!regResp) { grid.innerHTML = '<div class="mx-empty">Registry unavailable.</div>'; return; }
  const registry = regResp.markets;
  const tiers = regResp.tiers;
  document.getElementById('mx-count').textContent = Object.keys(registry).length;
  document.getElementById('kpi-markets').textContent = Object.keys(registry).length;

  // Merge live data (list) and gdci scores onto registry by slug
  const liveMap = {};
  if (listResp) {
    const rows = listResp.data || listResp.markets || listResp;
    if (Array.isArray(rows)) rows.forEach(r => { if (r.slug) liveMap[r.slug] = r; });
  }
  const gdciMap = {};
  if (gdciResp) {
    const rows = gdciResp.data || gdciResp.markets || gdciResp;
    if (Array.isArray(rows)) rows.forEach(r => { if (r.slug || r.market) gdciMap[r.slug || r.market] = r; });
  }

  // Aggregate KPIs
  let totalPipeline = 0, movers = 0;
  Object.values(liveMap).forEach(r => {
    if (r.pipeline_mw) totalPipeline += Number(r.pipeline_mw) || 0;
    if (r.weekly_change && Math.abs(r.weekly_change) > 2) movers += 1;
  });
  if (totalPipeline) document.getElementById('kpi-pipeline').textContent = (totalPipeline/1000).toFixed(1) + ' GW';
  if (movers) document.getElementById('kpi-movers').textContent = movers;
  document.getElementById('kpi-updated').textContent = new Date().toLocaleTimeString();

  // Build cards
  const cards = Object.entries(registry).map(([slug, m]) => {
    const live = liveMap[slug] || {};
    const gdci = gdciMap[slug] || {};
    const tier = tiers[m.tier] || { label: m.tier, color: '#3b82f6' };
    const delta = live.weekly_change || gdci.weekly_change || null;
    const badge = delta > 5 ? '<span class="mkt-badge mkt-hot">▲ ' + delta + '</span>'
                 : delta > 0 ? '<span class="mkt-badge mkt-up">▲ ' + delta + '</span>'
                 : '';
    const stats = [
      ['Facilities', live.facilities_count ? live.facilities_count : (m.operators?.length ? m.operators.length + '+' : '—')],
      ['Pipeline', live.pipeline_mw ? Math.round(live.pipeline_mw) + ' MW' : '—'],
      [gdci.score ? 'GDCI' : '$/kWh', gdci.score ? gdci.score : (live.avg_price_kwh ? '$' + Number(live.avg_price_kwh).toFixed(3) : '—')],
    ];
    const altSearch = [slug, m.name, m.country, m.state || '', m.iso || '', (m.alt_names||[]).join(' '), (m.operators||[]).join(' ')].join(' ').toLowerCase();
    const card = document.createElement('a');
    card.className = 'mkt-card';
    card.href = slug;
    card.dataset.tier = m.tier;
    card.dataset.search = altSearch;
    card.dataset.sortPipeline = live.pipeline_mw || 0;
    card.dataset.sortPrice = live.avg_price_kwh || 0;
    card.dataset.sortGdci = gdci.score || 0;
    card.innerHTML = `
      ${badge}
      <div class="mkt-top">
        <span class="mkt-flag">${m.flag}</span>
        <span class="mkt-name">${m.name}</span>
        <span class="mkt-tier" style="background:${tier.color}1f;color:${tier.color}">${tier.label}</span>
      </div>
      <div class="mkt-tl">${m.tagline || ''}</div>
      <div class="mkt-desc">${m.description || ''}</div>
      <div class="mkt-stats">
        ${stats.map(([l,v]) => `<div class="mkt-stat"><div class="mv">${v}</div><div class="ml">${l}</div></div>`).join('')}
      </div>
    `;
    return card;
  });

  function render() {
    grid.innerHTML = '';
    let shown = cards.slice();
    if (state.tier !== 'all') shown = shown.filter(c => c.dataset.tier === state.tier);
    if (state.q) {
      const q = state.q.toLowerCase();
      shown = shown.filter(c => c.dataset.search.includes(q));
    }
    const sortMap = {
      tier:     (a, b) => (tiers[a.dataset.tier]?.order||99) - (tiers[b.dataset.tier]?.order||99) || a.dataset.search.localeCompare(b.dataset.search),
      alpha:    (a, b) => a.dataset.search.localeCompare(b.dataset.search),
      gdci:     (a, b) => Number(b.dataset.sortGdci) - Number(a.dataset.sortGdci),
      pipeline: (a, b) => Number(b.dataset.sortPipeline) - Number(a.dataset.sortPipeline),
      price:    (a, b) => Number(a.dataset.sortPrice||999) - Number(b.dataset.sortPrice||999),
    };
    shown.sort(sortMap[state.sort]);
    if (!shown.length) {
      grid.innerHTML = '<div class="mx-empty">No markets match. Try a different filter or search term.</div>';
      return;
    }
    shown.forEach(c => grid.appendChild(c));
  }

  searchEl.addEventListener('input', e => { state.q = e.target.value.trim(); render(); });
  sortEl.addEventListener('change', e => { state.sort = e.target.value; render(); });
  chipsEl.addEventListener('click', e => {
    const c = e.target.closest('.chip'); if (!c) return;
    chipsEl.querySelectorAll('.chip').forEach(x => x.classList.remove('active'));
    c.classList.add('active');
    state.tier = c.dataset.tier;
    render();
  });

  render();
})();
</script>
</body>
</html>
