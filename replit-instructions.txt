DC Hub Replit AI Rules — DO NOT VIOLATE
CRITICAL: NEVER CHANGE THESE

OAuth Config — Do NOT modify Google OAuth redirect_uri, HMAC state verification, or callback handling in /api/auth/google/*. The flow uses stateless HMAC-signed state tokens (not cookies). redirect_uri MUST be https://dchub.cloud/api/auth/google/callback.
Database Connection — Do NOT change the Neon PostgreSQL connection string, table names, or column names. Tables: facilities, deals, news_articles, capacity_pipeline, ecosystem_companies, users. The Cloudflare Worker queries these tables directly via Neon HTTP API.
API Route Paths — Do NOT rename, move, or remove existing API routes. Frontend code depends on exact paths:

/api/health, /api/v1/stats, /api/news/live, /api/agent/*
/api/auth/google/redirect, /api/auth/google/callback, /api/auth/me
/api/v1/deals, /api/v1/map, /api/transactions/public
/api/founding-members, /api/ecosystem, /api/market-report


CORS Headers — All API responses MUST include Access-Control-Allow-Origin: *. Do NOT remove or restrict CORS headers.
JWT Token Format — Do NOT change the JWT signing key, payload structure (user_id, email, role, exp, iat), or token storage. Frontend stores token in localStorage as dchub_token and session as dchub_session.

ALLOWED: Auto-Discovery & Outreach

Adding NEW facilities to the facilities table ✅
Adding NEW news articles to news_articles ✅
Running KMZ/API discovery for power & gas data ✅
Adding NEW API routes (but never modifying existing ones) ✅
Database index changes (CREATE INDEX) ✅

BEFORE ANY CHANGE

Ask yourself: "Does this modify an existing route, table schema, or auth flow?"
If YES → STOP and confirm with the user first
If NO (purely additive) → proceed

ARCHITECTURE REMINDER

Frontend: Cloudflare Pages (static HTML/JS/CSS)
API Proxy: Cloudflare Worker dchubapiproxy queries Neon directly for read-only GET endpoints
Backend: This Replit app (Flask) handles writes, auth, AI queries, MCP
Auth tokens flow: Replit creates JWT → frontend stores in localStorage → Cloudflare Worker passes through to Replit for auth-required endpoints